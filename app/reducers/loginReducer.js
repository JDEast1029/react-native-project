/**
 * 登录路由
 */
const initState = {
    isLogin: false
};

const loginReducer = (state = initState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default loginReducer;